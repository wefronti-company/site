### Environment variables (do NOT store real secrets here in this example file)

# Next.js public (client) Google Analytics measurement ID (GA4) - visible to client
# e.g. NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
NEXT_PUBLIC_GA_ID=

# GA4 property id used by the server-side Data API. Numeric value only (e.g. 123456789)
GA4_PROPERTY_ID=

# Option A: server-side Service Account JSON (single-line JSON string). Preferred for Data API.
# For example: GA4_SERVICE_ACCOUNT_JSON="{...}" — store this securely in your deployment secrets.
GA4_SERVICE_ACCOUNT_JSON=

# Option B: alternatively your hosting platform may provide GOOGLE_APPLICATION_CREDENTIALS
# as a file path; use whatever your host recommends. Do NOT commit service account JSON.

# Cloudflare / CDN (optional) - used by the cache-clear endpoint to purge CDN
# CLOUDFLARE_ZONE_ID=xxxxxxxxxxxxxxxxxxxx
# CLOUDFLARE_API_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Fastly (optional)
# FASTLY_API_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# FASTLY_SERVICE_ID=xxxxxxxxxxxxxxxxxxxxxxxx

# AWS CloudFront (optional)
# CLOUDFRONT_DISTRIBUTION_ID=E1234567890
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1

# Default caching values used by middleware (seconds)
# DEFAULT_S_MAXAGE=60
# DEFAULT_STALE=300

# Optional: flags for admin 2FA detection
# ADMIN_ENFORCE_2FA=true

# Realtime GA API cache TTL seconds (short)
GA_REALTIME_CACHE_TTL_SECONDS=15

 