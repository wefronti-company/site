// Backward-compatible re-export: canonical component is `Header` (components/ui/Header.tsx).
// DEPRECATION: keep importing from `components/ui/Menu` for now but update to `components/ui/Header`.

export { default } from './Header';

