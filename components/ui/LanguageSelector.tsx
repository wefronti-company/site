// Language selection UI intentionally removed.
// The site is fixed to pt-BR; this component is kept as a stub to avoid accidental imports.
import React from 'react';

const LanguageSelector: React.FC = () => null;

export default LanguageSelector;
