{
  "name": "wefronti",
  "private": true,
  "workspaces": [
    "site",
    "dashboard",
    "console"
  ],
  "scripts": {
    "dev:site": "npm --workspace site run dev",
    "dev:dashboard": "npm --workspace dashboard run dev",
    "dev:console": "npm --workspace console run dev",
    "build": "npm --workspaces run build",
    "start": "npm --workspaces run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
